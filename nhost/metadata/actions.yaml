actions:
  - name: beer_defaults
    definition:
      kind: ""
      handler: '{{NHOST_FUNCTIONS_URL}}/getItemDefaults'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 120
      request_transform:
        body:
          action: transform
          template: |-
            {
              "hint": {
                "barcode": {{$body.input.hint?.barcode}},
                "barcodeType": {{$body.input.hint?.barcodeType}},
                "frontLabelFileId": {{$body.input.hint?.frontLabelFileId}},
                "backLabelFileId": {{$body.input.hint?.backLabelFileId}}
              },
              "itemType": "BEER",
              "session_variables": {{$body?.session_variables}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
    comment: beer_defaults
  - name: coffee_defaults
    definition:
      kind: ""
      handler: '{{NHOST_FUNCTIONS_URL}}/getItemDefaults'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 120
      request_transform:
        body:
          action: transform
          template: |-
            {
              "hint": {
                "barcode": {{$body.input.hint?.barcode}},
                "barcodeType": {{$body.input.hint?.barcodeType}},
                "frontLabelFileId": {{$body.input.hint?.frontLabelFileId}},
                "backLabelFileId": {{$body.input.hint?.backLabelFileId}}
              },
              "itemType": "COFFEE",
              "session_variables": {{$body?.session_variables}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: create_search_vector
    definition:
      kind: ""
      handler: '{{NHOST_FUNCTIONS_URL}}/getVectorForString'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
    permissions:
      - role: user
  - name: enrich_place_from_google
    definition:
      kind: synchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/enrichPlaceFromGoogle'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 60
      request_transform:
        body:
          action: transform
          template: |-
            {
              "placeId": {{$body.input.input?.placeId}},
              "googlePlaceId": {{$body.input.input?.googlePlaceId}},
              "maxPhotos": {{$body.input.input?.maxPhotos}},
              "triggeredBy": {{$body.session_variables.x-hasura-user-id}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: google_autocomplete
    definition:
      kind: synchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/googlePlacesAutocomplete'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 15
      request_transform:
        body:
          action: transform
          template: |-
            {
              "input": {{$body.input.input.input}},
              "latitude": {{$body.input.input.latitude}},
              "longitude": {{$body.input.input.longitude}},
              "triggeredBy": {{$body.session_variables.x-hasura-user-id}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: google_nearby_search
    definition:
      kind: synchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/googlePlacesNearbySearch'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 15
      request_transform:
        body:
          action: transform
          template: |-
            {
              "latitude": {{$body.input.input.latitude}},
              "longitude": {{$body.input.input.longitude}},
              "triggeredBy": {{$body.session_variables.x-hasura-user-id}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: item_image_upload
    definition:
      kind: synchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/uploadItemImage'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      request_transform:
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: processRecipePhoto
    definition:
      kind: synchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/processRecipePhoto'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 120
      request_transform:
        body:
          action: transform
          template: |-
            {
              "photoId": {{$body.input.input.photoId}},
              "userId": {{$body.input.input.userId}},
              "placeId": {{$body.input.input.placeId}},
              "menuItemId": {{$body.input.input.menuItemId}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: refresh_places
    definition:
      kind: asynchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/refreshPlaces'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 300
      request_transform:
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: admin
  - name: reviewUserPlace
    definition:
      kind: synchronous
      handler: '{{NHOST_FUNCTIONS_URL}}/reviewUserPlace'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 15
      request_transform:
        body:
          action: transform
          template: |-
            {
              "name": {{$body.input.input.name}},
              "categories": {{$body.input.input.categories}},
              "latitude": {{$body.input.input.latitude}},
              "longitude": {{$body.input.input.longitude}},
              "street_address": {{$body.input.input?.street_address}},
              "locality": {{$body.input.input?.locality}},
              "region": {{$body.input.input?.region}},
              "country_code": {{$body.input.input?.country_code}},
              "phone": {{$body.input.input?.phone}},
              "website": {{$body.input.input?.website}},
              "description": {{$body.input.input?.description}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: sake_defaults
    definition:
      kind: ""
      handler: '{{NHOST_FUNCTIONS_URL}}/getItemDefaults'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 120
      request_transform:
        body:
          action: transform
          template: |-
            {
              "hint": {
                "barcode": {{$body.input.hint?.barcode}},
                "barcodeType": {{$body.input.hint?.barcodeType}},
                "frontLabelFileId": {{$body.input.hint?.frontLabelFileId}},
                "backLabelFileId": {{$body.input.hint?.backLabelFileId}}
              },
              "itemType": "SAKE",
              "session_variables": {{$body?.session_variables}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
    comment: sake_defaults
  - name: search_nearby_places
    definition:
      kind: ""
      handler: '{{NHOST_FUNCTIONS_URL}}/searchNearbyPlaces'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 35
      request_transform:
        body:
          action: transform
          template: |-
            {
              "latitude": {{$body.input.input.latitude}},
              "longitude": {{$body.input.input.longitude}},
              "radius": {{$body.input.input.radius}},
              "categories": {{$body.input.input.categories}},
              "limit": {{$body.input.input.limit}},
              "session_variables": {{$body.session_variables}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
  - name: spirit_defaults
    definition:
      kind: ""
      handler: '{{NHOST_FUNCTIONS_URL}}/getItemDefaults'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 118
      request_transform:
        body:
          action: transform
          template: |-
            {
              "hint": {
                "barcode": {{$body.input.hint?.barcode}},
                "barcodeType": {{$body.input.hint?.barcodeType}},
                "frontLabelFileId": {{$body.input.hint?.frontLabelFileId}},
                "backLabelFileId": {{$body.input.hint?.backLabelFileId}}
              },
              "itemType": "SPIRIT",
              "session_variables": {{$body?.session_variables}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
    comment: spirit_defaults
  - name: wine_defaults
    definition:
      kind: ""
      handler: '{{NHOST_FUNCTIONS_URL}}/getItemDefaults'
      forward_client_headers: true
      headers:
        - name: nhost-webhook-secret
          value_from_env: NHOST_WEBHOOK_SECRET
      timeout: 118
      request_transform:
        body:
          action: transform
          template: |-
            {
              "hint": {
                "barcode": {{$body.input.hint?.barcode}},
                "barcodeType": {{$body.input.hint?.barcodeType}},
                "frontLabelFileId": {{$body.input.hint?.frontLabelFileId}},
                "backLabelFileId": {{$body.input.hint?.backLabelFileId}}
              },
              "itemType": "WINE",
              "session_variables": {{$body?.session_variables}}
            }
        method: POST
        query_params: {}
        template_engine: Kriti
        version: 2
    permissions:
      - role: user
custom_types:
  enums:
    - name: ItemType
      values:
        - description: null
          is_deprecated: null
          value: BEER
        - description: null
          is_deprecated: null
          value: WINE
        - description: null
          is_deprecated: null
          value: SPIRIT
        - description: null
          is_deprecated: null
          value: COFFEE
        - description: null
          is_deprecated: null
          value: SAKE
  input_objects:
    - name: item_defaults_input
    - name: item_defaults_hint
    - name: item_image_upload_input
    - name: search_nearby_places_input
    - name: ProcessRecipePhotoInput
    - name: ReviewUserPlaceInput
    - name: GoogleNearbySearchInput
    - name: GoogleAutocompleteInput
    - name: EnrichPlaceFromGoogleInput
  objects:
    - name: wine_defaults_result
    - name: beer_defaults_result
    - name: spirit_defaults_result
    - name: item_image_upload_result
    - name: coffee_defaults_result
    - name: sake_defaults_result
    - name: search_nearby_places_result
    - name: search_nearby_place
    - name: search_query_info
    - name: search_location_info
    - name: refresh_places_result
    - name: ProcessRecipePhotoOutput
    - name: ProcessRecipePhotoResult
    - name: MenuAnalysis
    - name: ReviewUserPlaceOutput
    - name: GoogleNearbySearchOutput
    - name: GoogleNearbyPlace
    - name: GoogleAutocompleteOutput
    - name: GoogleAutocompleteSuggestion
    - name: EnrichPlaceFromGoogleOutput
    - name: GoogleEnrichmentData
    - name: GooglePlacePhotoResult
  scalars: []
