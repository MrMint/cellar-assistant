table:
  name: instruction_types
  schema: public
is_enum: true
array_relationships:
  - name: recipe_instructions
    using:
      foreign_key_constraint_on:
        column: instruction_type
        table:
          name: recipe_instructions
          schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - comment
      filter: {}
    comment: ""
